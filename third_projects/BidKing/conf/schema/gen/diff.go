// Code generated by gen_config. DO NOT EDIT.
package gen

import "reflect"

func (c *CommonConfig) CheckStatic(old *CommonConfig) []string {
	var stale []string
	if old == nil || c == nil { return []string{"<nil>"} }
	if c.Node != nil && old.Node != nil {
		stale = append(stale, prefixAll(c.Node.CheckStatic(old.Node), "node.")...)
	} else if c.Node != old.Node {
		stale = append(stale, "node")
	}
	if c.Etcd != nil && old.Etcd != nil {
		stale = append(stale, prefixAll(c.Etcd.CheckStatic(old.Etcd), "etcd.")...)
	} else if c.Etcd != old.Etcd {
		stale = append(stale, "etcd")
	}
	if c.Redis != nil && old.Redis != nil {
		stale = append(stale, prefixAll(c.Redis.CheckStatic(old.Redis), "redis.")...)
	} else if c.Redis != old.Redis {
		stale = append(stale, "redis")
	}
	return stale
}

func (c *EtcdConfig) CheckStatic(old *EtcdConfig) []string {
	var stale []string
	if old == nil || c == nil { return []string{"<nil>"} }
	if !reflect.DeepEqual(c.Endpoints, old.Endpoints) { stale = append(stale, "endpoints") }
	return stale
}

func (c *GateConfig) CheckStatic(old *GateConfig) []string {
	var stale []string
	if old == nil || c == nil { return []string{"<nil>"} }
	if c.ListenTcp != old.ListenTcp { stale = append(stale, "listen_tcp") }
	if c.ListenWs != old.ListenWs { stale = append(stale, "listen_ws") }
	if c.DrainTimeoutSec != old.DrainTimeoutSec { stale = append(stale, "drain_timeout_sec") }
	return stale
}

func (c *GatesvrConfig) CheckStatic(old *GatesvrConfig) []string {
	var stale []string
	if old == nil || c == nil { return []string{"<nil>"} }
	if c.Gate != nil && old.Gate != nil {
		stale = append(stale, prefixAll(c.Gate.CheckStatic(old.Gate), "gate.")...)
	} else if c.Gate != old.Gate {
		stale = append(stale, "gate")
	}
	if c.Log != nil && old.Log != nil {
		stale = append(stale, prefixAll(c.Log.CheckStatic(old.Log), "log.")...)
	} else if c.Log != old.Log {
		stale = append(stale, "log")
	}
	return stale
}

func (c *LobbyConfig) CheckStatic(old *LobbyConfig) []string {
	var stale []string
	if old == nil || c == nil { return []string{"<nil>"} }
	return stale
}

func (c *LobbysvrConfig) CheckStatic(old *LobbysvrConfig) []string {
	var stale []string
	if old == nil || c == nil { return []string{"<nil>"} }
	if c.Lobby != nil && old.Lobby != nil {
		stale = append(stale, prefixAll(c.Lobby.CheckStatic(old.Lobby), "lobby.")...)
	} else if c.Lobby != old.Lobby {
		stale = append(stale, "lobby")
	}
	if c.Log != nil && old.Log != nil {
		stale = append(stale, prefixAll(c.Log.CheckStatic(old.Log), "log.")...)
	} else if c.Log != old.Log {
		stale = append(stale, "log")
	}
	return stale
}

func (c *LogConfig) CheckStatic(old *LogConfig) []string {
	var stale []string
	if old == nil || c == nil { return []string{"<nil>"} }
	if c.Level != old.Level { stale = append(stale, "level") }
	if c.Format != old.Format { stale = append(stale, "format") }
	if c.StderrAlso != old.StderrAlso { stale = append(stale, "stderr_also") }
	if c.Dir != old.Dir { stale = append(stale, "dir") }
	if c.Basename != old.Basename { stale = append(stale, "basename") }
	if c.MaxSizeMb != old.MaxSizeMb { stale = append(stale, "max_size_mb") }
	if c.MaxBackups != old.MaxBackups { stale = append(stale, "max_backups") }
	if c.RotateByHour != old.RotateByHour { stale = append(stale, "rotate_by_hour") }
	return stale
}

func (c *LogGroupConfig) CheckStatic(old *LogGroupConfig) []string {
	var stale []string
	if old == nil || c == nil { return []string{"<nil>"} }
	if c.Main != nil && old.Main != nil {
		stale = append(stale, prefixAll(c.Main.CheckStatic(old.Main), "main.")...)
	} else if c.Main != old.Main {
		stale = append(stale, "main")
	}
	if c.Res != nil && old.Res != nil {
		stale = append(stale, prefixAll(c.Res.CheckStatic(old.Res), "res.")...)
	} else if c.Res != old.Res {
		stale = append(stale, "res")
	}
	if c.Tracing != nil && old.Tracing != nil {
		stale = append(stale, prefixAll(c.Tracing.CheckStatic(old.Tracing), "tracing.")...)
	} else if c.Tracing != old.Tracing {
		stale = append(stale, "tracing")
	}
	return stale
}

func (c *MatchConfig) CheckStatic(old *MatchConfig) []string {
	var stale []string
	if old == nil || c == nil { return []string{"<nil>"} }
	return stale
}

func (c *MatchsvrConfig) CheckStatic(old *MatchsvrConfig) []string {
	var stale []string
	if old == nil || c == nil { return []string{"<nil>"} }
	if c.Match != nil && old.Match != nil {
		stale = append(stale, prefixAll(c.Match.CheckStatic(old.Match), "match.")...)
	} else if c.Match != old.Match {
		stale = append(stale, "match")
	}
	if c.Log != nil && old.Log != nil {
		stale = append(stale, prefixAll(c.Log.CheckStatic(old.Log), "log.")...)
	} else if c.Log != old.Log {
		stale = append(stale, "log")
	}
	return stale
}

func (c *NodeConfig) CheckStatic(old *NodeConfig) []string {
	var stale []string
	if old == nil || c == nil { return []string{"<nil>"} }
	if c.WorldId != old.WorldId { stale = append(stale, "world_id") }
	if c.ServerType != old.ServerType { stale = append(stale, "server_type") }
	if c.ServerIndex != old.ServerIndex { stale = append(stale, "server_index") }
	return stale
}

func (c *OnlineConfig) CheckStatic(old *OnlineConfig) []string {
	var stale []string
	if old == nil || c == nil { return []string{"<nil>"} }
	return stale
}

func (c *OnlinesvrConfig) CheckStatic(old *OnlinesvrConfig) []string {
	var stale []string
	if old == nil || c == nil { return []string{"<nil>"} }
	if c.Online != nil && old.Online != nil {
		stale = append(stale, prefixAll(c.Online.CheckStatic(old.Online), "online.")...)
	} else if c.Online != old.Online {
		stale = append(stale, "online")
	}
	if c.Log != nil && old.Log != nil {
		stale = append(stale, prefixAll(c.Log.CheckStatic(old.Log), "log.")...)
	} else if c.Log != old.Log {
		stale = append(stale, "log")
	}
	return stale
}

func (c *RedisConfig) CheckStatic(old *RedisConfig) []string {
	var stale []string
	if old == nil || c == nil { return []string{"<nil>"} }
	if c.Host != old.Host { stale = append(stale, "host") }
	if c.Port != old.Port { stale = append(stale, "port") }
	if c.Password != old.Password { stale = append(stale, "password") }
	return stale
}

func (c *RoomConfig) CheckStatic(old *RoomConfig) []string {
	var stale []string
	if old == nil || c == nil { return []string{"<nil>"} }
	return stale
}

func (c *RoomsvrConfig) CheckStatic(old *RoomsvrConfig) []string {
	var stale []string
	if old == nil || c == nil { return []string{"<nil>"} }
	if c.Room != nil && old.Room != nil {
		stale = append(stale, prefixAll(c.Room.CheckStatic(old.Room), "room.")...)
	} else if c.Room != old.Room {
		stale = append(stale, "room")
	}
	if c.Log != nil && old.Log != nil {
		stale = append(stale, prefixAll(c.Log.CheckStatic(old.Log), "log.")...)
	} else if c.Log != old.Log {
		stale = append(stale, "log")
	}
	return stale
}

func (c *RouterAgentConfig) CheckStatic(old *RouterAgentConfig) []string {
	var stale []string
	if old == nil || c == nil { return []string{"<nil>"} }
	return stale
}

func (c *RouteragentConfig) CheckStatic(old *RouteragentConfig) []string {
	var stale []string
	if old == nil || c == nil { return []string{"<nil>"} }
	if c.RouterAgent != nil && old.RouterAgent != nil {
		stale = append(stale, prefixAll(c.RouterAgent.CheckStatic(old.RouterAgent), "router_agent.")...)
	} else if c.RouterAgent != old.RouterAgent {
		stale = append(stale, "router_agent")
	}
	if c.Log != nil && old.Log != nil {
		stale = append(stale, prefixAll(c.Log.CheckStatic(old.Log), "log.")...)
	} else if c.Log != old.Log {
		stale = append(stale, "log")
	}
	return stale
}

